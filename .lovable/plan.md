# Increase mobile gap below "50+ languages" title

In `src/components/asmi/Act5.tsx`, change the title wrapper margin from `mb-8 md:mb-12` to `mb-12 md:mb-16` so the "50+ languages" heading has more breathing room before the mobile language cloud starts.

File touched: `src/components/asmi/Act5.tsx` (one class change). No new deps.
